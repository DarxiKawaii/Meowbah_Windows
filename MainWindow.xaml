<?xml version="1.0" encoding="utf-8"?>
<win:Window
    x:Class="Meowbah.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Meowbah"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:win="using:Microsoft.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Meowbah">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- App title bar (separate, sits above the NavigationView) -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="32" Background="Transparent">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
                <TextBlock Text="Meowbah" FontSize="14" FontWeight="SemiBold" Foreground="White" />
            </StackPanel>
        </Grid>

        <!-- Top navigation pane (PaneDisplayMode="Top") -->
        <muxc:NavigationView x:Name="ShellNavView" Grid.Row="1"
                              PaneDisplayMode="Top"
                              IsSettingsVisible="False"
                              IsBackButtonVisible="Collapsed"
                              SelectionChanged="ShellNavView_SelectionChanged">

            <muxc:NavigationView.MenuItems>
                <muxc:NavigationViewItem x:Name="NavVideos" Content="Videos" Tag="Videos">
                    <muxc:NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Play" />
                    </muxc:NavigationViewItem.Icon>
                </muxc:NavigationViewItem>

                <muxc:NavigationViewItem x:Name="NavArt" Content="Art" Tag="Art">
                    <muxc:NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Camera" />
                    </muxc:NavigationViewItem.Icon>
                </muxc:NavigationViewItem>

                <muxc:NavigationViewItem x:Name="NavMerch" Content="Merch" Tag="Merch">
                    <muxc:NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Shop" />
                    </muxc:NavigationViewItem.Icon>
                </muxc:NavigationViewItem>
            </muxc:NavigationView.MenuItems>

        </muxc:NavigationView>

        <!-- Cart button overlayed on the top nav row so it is always visible and not clipped by the NavigationView template -->
        <Button x:Name="CartButton" Grid.Row="1" Click="CartButton_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" Spacing="6">
                <SymbolIcon Symbol="Shop" />
                <TextBlock x:Name="CartCountText" Text="0" />
            </StackPanel>
        </Button>

        <!-- Content frame placed in main grid row 2 so it sits directly below the NavigationView pane -->
        <Frame x:Name="ContentFrame" Grid.Row="2" />
    </Grid>
</win:Window>
