<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Meowbah.MerchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Meowbah"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16,0,16,16">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="Wide">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="1200" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="ItemsWrapGridPanel.ItemWidth" Value="300" />
                        </VisualState.Setters>
                    </VisualState>

                    <VisualState x:Name="Normal">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="800" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="ItemsWrapGridPanel.ItemWidth" Value="300" />
                        </VisualState.Setters>
                    </VisualState>

                    <VisualState x:Name="Narrow">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="0" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="ItemsWrapGridPanel.ItemWidth" Value="360" />
                        </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="ExtraWide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1600" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ItemsWrapGridPanel.ItemWidth" Value="420" />
                        <Setter Target="MerchGridView.Padding" Value="24,0,160,24" />
                    </VisualState.Setters>
                </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" FontSize="28" FontWeight="SemiBold" Text="Merch" Margin="0,0,0,12" />

                <!-- GridView bound to MerchItems to display items in multiple columns; outer ScrollViewer handles scrolling -->
                <GridView x:Name="MerchGridView" Grid.Row="1" ItemsSource="{x:Bind MerchItems}" IsItemClickEnabled="False" SelectionMode="None"
                          VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                          Padding="24,0,220,24" Margin="0"
                          HorizontalContentAlignment="Left"
                          ScrollViewer.VerticalScrollMode="Enabled"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollMode="Disabled"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem">
                            <!-- horizontal,vertical margins to space items more without resizing them -->
                            <Setter Property="Margin" Value="32,40" />
                        </Style>
                    </GridView.ItemContainerStyle>
                    <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                            <ItemsWrapGrid x:Name="ItemsWrapGridPanel" Orientation="Horizontal" ItemWidth="360" HorizontalAlignment="Left" />
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>

                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="local:MerchItem">
                            <Grid Padding="8" ColumnSpacing="16">
                                <!-- Replaced ColumnDefinitions attribute with explicit ColumnDefinition elements -->
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="150" Height="150" CornerRadius="8" Background="#FFF0F0F0" HorizontalAlignment="Left" VerticalAlignment="Top">
                                    <Image Source="{x:Bind ImageSource}" Stretch="UniformToFill" />
                                </Border>

                                <StackPanel Grid.Column="1" Margin="0,6,0,0" VerticalAlignment="Top">
                                    <TextBlock Text="{x:Bind Title}" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                                    <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap" Margin="0,8,0,0" />
                                    <TextBlock Text="{x:Bind Price}" FontSize="16" FontWeight="SemiBold" Margin="0,10,0,0" />
                                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                        <Button x:Name="AddToCartButton" Content="Add to Cart" Click="AddToCartButton_Click" IsEnabled="{x:Bind CanAddToCart}" />
                                        <TextBlock Text="Sold Out" Foreground="Gray" Margin="8,4,0,0" Visibility="{x:Bind SoldOutVisibility}" />
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </Grid>
        </Grid>
</Page>
